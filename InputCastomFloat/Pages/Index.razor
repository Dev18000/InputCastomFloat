@using InputCastomFloat.Shared
@page "/"

<NumericInput Title="Введите число" Value="@value" ValueChanged="OnValueChanged" ErrorMessage="@errorMessage" />

@code {
    private string value = "";
    private string errorMessage = "";

    private void OnValueChanged(string newValue)
    {
        value = newValue;
    }
}